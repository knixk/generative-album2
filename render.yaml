services:
  - type: web
    name: backend
    env: node
    buildCommand: cd backend && npm install
    startCommand: node index.js
    envVars:
      - key: PORT
        value: 5051
      - key: SOCKET_IO_PORT
        value: 3000
